<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.mapper.RootMapper">
    <select id="login" resultType="Root">
        SELECT * FROM root WHERE admin=#{0} AND password=#{1};
    </select>
</mapper>